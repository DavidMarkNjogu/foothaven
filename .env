# Application environment
APP_ENV=development
APP_DEBUG=true

# Database settings (copied from db.php)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=foothaven

# Optional settings
APP_URL=http://localhost/foothaven

# Note: Keep this file out of version control (add to .gitignore)
